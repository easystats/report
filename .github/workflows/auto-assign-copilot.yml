# .github/workflows/auto-assign-copilot.yaml
name: Auto-assign Copilot on Copilot tasks
on:
  issues:
    types: [opened]

jobs:
  assign:
    name: Assign Copilot to task
    runs-on: ubuntu-latest
    permissions:
      issues: write
    steps:
      - uses: actions/github-script@v7
        with:
          script: |
            const issue = context.payload.issue
            
            console.log('🤖 Auto-assign Copilot workflow started')
            console.log(`📋 Issue #${issue.number}: "${issue.title}"`)
            console.log(`🏷️  Labels: ${issue.labels.map(l => l.name).join(', ')}`)
            console.log(`👤 Issue author: ${issue.user.login}`)
            console.log(`🏠 Repository: ${context.repo.owner}/${context.repo.repo}`)
            
            // Adjust condition to your template (label, title prefix, or issue type)
            const titleMatches = issue.title.startsWith("[Copilot]: ")
            const labelMatches = issue.labels.some(l => l.name.toLowerCase() === "robot :robot:")
            const isCopilotTask = titleMatches || labelMatches
            
            console.log(`🔍 Title starts with "[Copilot]: ": ${titleMatches}`)
            console.log(`🔍 Has "robot :robot:" label: ${labelMatches}`)
            console.log(`✅ Is Copilot task: ${isCopilotTask}`)

            if (isCopilotTask) {
              console.log('🎯 Attempting to assign Copilot to the issue...')
              try {
                // Use the correct Copilot username (uppercase - matches actual bot login ID 198982749)
                console.log('🔍 Assigning to: "Copilot" (Bot ID 198982749)')
                const assignmentResponse = await github.rest.issues.addAssignees({
                  owner: context.repo.owner,
                  repo: context.repo.repo,
                  issue_number: issue.number,
                  assignees: ["Copilot"]  // Correct username for the GitHub Copilot bot
                })
                
                // Wait a moment for the assignment to take effect
                await new Promise(resolve => setTimeout(resolve, 2000))
                
                // CRITICAL: Always verify assignment actually worked 
                console.log('🔍 Verifying assignment took effect...')
                const updatedIssue = await github.rest.issues.get({
                  owner: context.repo.owner,
                  repo: context.repo.repo,
                  issue_number: issue.number
                })
                
                const assignees = updatedIssue.data.assignees || []
                const assigneeList = assignees.map(a => `${a.login} (${a.type})`)
                console.log(`📋 Current assignees: ${assigneeList.length > 0 ? assigneeList.join(', ') : 'none'}`)
                
                // Check if Copilot is actually assigned (Bot ID 198982749)
                const copilotAssigned = assignees.some(a => 
                  a.id === 198982749 || (a.login === 'Copilot' && a.type === 'Bot')
                )
                
                if (copilotAssigned) {
                  console.log('✅ SUCCESS: Copilot is actually assigned to the issue!')
                } else {
                  // Assignment API call succeeded but assignment didn't take effect
                  console.log('❌ FAILURE: Assignment API succeeded but Copilot is NOT actually assigned')
                  console.log('🔍 This indicates the "Copilot" username may not be accessible or valid')
                  throw new Error('Assignment verification failed - Copilot not found in assignees list')
                }
                
              } catch (e) {
                console.log(`❌ Failed to assign Copilot: ${e.message}`)
                console.log(`🔍 Error details: ${JSON.stringify(e, null, 2)}`)
                console.log('🔄 Falling back to assign rempsyc...')
                try {
                  // Fallback: assign to rempsyc and keep the label so manual assignment can be done
                  await github.rest.issues.addAssignees({
                    owner: context.repo.owner,
                    repo: context.repo.repo,
                    issue_number: issue.number,
                    assignees: ["rempsyc"]
                  })
                  console.log('✅ Successfully assigned rempsyc as fallback!')
                  console.log('💡 User will need to manually assign Copilot from the GitHub UI')
                } catch (fallbackError) {
                  console.log(`❌ Failed to assign fallback rempsyc: ${fallbackError.message}`)
                  console.log(`🔍 Fallback error details: ${JSON.stringify(fallbackError, null, 2)}`)
                  console.log('💡 Both assignment attempts failed - this indicates a deeper permissions issue')
                  throw fallbackError
                }
              }
            } else {
              console.log('⏭️  Skipping assignment - not a Copilot task')
            }
            
            console.log('🏁 Auto-assign Copilot workflow completed')
